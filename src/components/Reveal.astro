---
// 滚动揭示包装器。delay 单位毫秒，用于错落入场。
interface Props {
  delay?: number;
  class?: string;
}
const { delay = 0, class: className = '' } = Astro.props;
---

<div class:list={['reveal', className]} style={`transition-delay:${delay}ms`}>
  <slot />
</div>
